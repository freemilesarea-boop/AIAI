# ── LUBER MUSIC AI — environment template ──────────────────────────────
# Copy to .env and adjust. Never commit a real .env file.

# Environment: development | test | production
ENVIRONMENT=development
LOG_LEVEL=INFO

# PostgreSQL (docker-compose service: postgres)
POSTGRES_USER=luber
POSTGRES_PASSWORD=luber_dev_password
POSTGRES_DB=luber
POSTGRES_PORT=5432
DATABASE_URL=postgresql+asyncpg://luber:luber_dev_password@localhost:5432/luber

# Redis (docker-compose service: redis)
REDIS_PORT=6379
REDIS_URL=redis://localhost:6379/0

# FastAPI backend
API_HOST=0.0.0.0
API_PORT=8000

# Next.js frontend
WEB_PORT=3000
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# Object storage (Phase 4+; S3-compatible — left empty in Phase 0)
STORAGE_ENDPOINT=
STORAGE_BUCKET=
STORAGE_ACCESS_KEY_ID=
STORAGE_SECRET_ACCESS_KEY=

# Generation provider (Phase 1+): mock | ace_step
GENERATION_PROVIDER=mock
