{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "infra/docker/worker.Dockerfile"
  },
  "deploy": {
    "startCommand": "arq luber_generation_worker.worker.WorkerSettings",
    "restartPolicyType": "ON_FAILURE",
    "numReplicas": 1
  }
}
