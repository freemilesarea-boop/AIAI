# Terraform

Infrastructure-as-code for cloud deployment (object storage, GPU worker
pools, managed PostgreSQL/Redis). Populated when production deployment
begins. No cloud resources are provisioned in Phase 0.
