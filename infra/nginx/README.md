# Nginx

Reverse-proxy / API-gateway configuration for production deployments
(TLS termination, routing to web + api). Populated during deployment
hardening (Phase 6+). Not required for local development — Compose
exposes ports directly.
