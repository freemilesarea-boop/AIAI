[project]
name = "luber-audio-utils"
version = "0.1.0"
description = "Audio constants and validation helpers (post-processing arrives in Phase 4)"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/luber_audio_utils"]
