[project]
name = "luber-dataset"
version = "0.1.0"
description = "Authorized training dataset schema, rights gate, and quality gates"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/luber_dataset"]
