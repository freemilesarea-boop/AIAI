"""The operator training console: read models, actions, and its boundary.

Everything in this package serves one internal console. Nothing here is
reachable by a product account, and nothing here is mounted in a
production process — see :mod:`luber_api.ops.security` for why the
boundary is the deployment rather than a role.
"""
